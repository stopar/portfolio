<div class="mx-auto w-full">

  <%= link_to sanitize("&larr; Kembali ke ahli"), member_path(@member), 
      class: "hover:text-blue-400" %>

  <div class="mt-10">
    <h1 class="font-bold text-3xl text-blue-400 text-left underline underline-offset-8">Pinjaman baru</h1>
  </div>
  
  <%= turbo_frame_tag @loan do %>
	  <%= render "form", member: @member, loan: @loan %>
  <% end %>

</div>