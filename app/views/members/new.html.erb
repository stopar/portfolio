<div class="mx-auto md:w-2/3 w-full">

  <%= link_to sanitize("&larr; Kembali ke semua ahli"), members_path, 
      class: "hover:text-blue-400" %>

  <div class="mt-10">
    <h1 class="font-bold text-3xl text-blue-400 text-center underline underline-offset-8">Ahli baru</h1>
  </div>

  <%= turbo_frame_tag @member do %>
    <%= render "form", member: @member %>
  <% end %>

</div>
